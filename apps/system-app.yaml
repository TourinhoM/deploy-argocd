apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: system-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/TourinhoM/deploy-argocd
    targetRevision: main
    path: apps/system
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
