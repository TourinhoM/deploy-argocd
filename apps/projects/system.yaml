apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: system
  namespace: argocd
spec:
  description: Componentes de plataforma do cluster (Argo CD self-management, External Secrets)
  sourceRepos:
    - https://github.com/TourinhoM/deploy-argocd
    - https://charts.external-secrets.io
  destinations:
    - namespace: argocd
      server: https://kubernetes.default.svc
    - namespace: external-secrets
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
